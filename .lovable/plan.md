

## Plan: Install Polotno SDK

Add the `polotno` package to the project dependencies.

### What will be done
- Add `polotno` to `package.json` dependencies

### Note
Polotno SDK is a canvas/design editor library. Once installed, it can be integrated into features like the Design Studio for more advanced card editing capabilities. After installation, we can discuss how you'd like to use it in the project.

